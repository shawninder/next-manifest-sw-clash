{
  "name": "create-next-example-app",
  "scripts": {
    "dev": "node server.js",
    "clean": "rm -rf .next static/manifest",
    "build": "next build",
    "prestart": "npm run clean && npm run build",
    "start": "NODE_ENV=production node server.js",
    "test": "jest"
  },
  "dependencies": {
    "mkdirp": "^0.5.1",
    "next": "7.0.2",
    "next-manifest": "^1.6.0",
    "next-offline": "git@github.com:shawninder/next-offline.git",
    "react": "16.6.1",
    "react-dom": "16.6.1",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
